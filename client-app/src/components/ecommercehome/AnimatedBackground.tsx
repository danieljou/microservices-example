"use client";
import React from 'react';

const AnimatedBackground = () => {
    return (
        <div className="parallax-bg">
            {/* Background animation styles can be applied here */}
        </div>
    );
};

export default AnimatedBackground;